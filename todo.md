## bug
- [x] `4.2.0` 下载过程中，task页面的图片选择老是被打断
- [x] `4.2.0` 画册面包屑点击应该replace而不是push
- [ ] PathQL 路径中自定义标签斜线出现解析失败问题

## issue
- [x] `4.3.0` 扩展cli功能，修复cli bug
    - 将CLI从ipc变为无ipc
    - 去掉cli运行v8 feature
- [x] `4.2.1` 浏览器不支持WMV，将该类格式视频同时转换MP4放在缓存目录，前端在访问该类视频的时候发给video的是wmv url，服务器返回的是mp4数据流。对于macos和linux，需要该servdir，对于windows来说需要修改自定制流式返回逻辑。安卓则由kotlin侧处理转换。
- [ ] 壁纸禁用功能和应用背景功能有些冲突。应该在壁纸禁用的时候也允许设置应用背景
- [x] `4.2.1` (仅窗口模式) 壁纸平铺不要原尺寸，而是适应+重复

## optimize
- [x] `4.2.0` 删除图片改成将图片移入系统回收站
- [ ] 添加设置窗口模态是否弹出的设置选项
- [ ] 插件安装失败显示失败原因
- [x] `4.2.0` （批次大小改成10）整理过程中，如果生成缩略图可能较慢，需要更细粒度的进度指示
- [x] `4.2.0` 安卓视频播放用photoswipe
- [x] `4.2.0` light模式去掉视频支持
- [x] `4.2.0` 视频支持用嵌入ffmpeg实现
- [x] `4.2.0` 整理过程不需要gate实际上
- [ ] 视频播放通过ctrl + 箭头可以seek，而不是切换视频
- [x] `4.2.1` 浏览器兼容格式
    ```prompt
        当前前端显示依赖于浏览器的能力，对于特别大的图片、视频、不支持的wmv格式等，浏览器不能够显示。
        1. 在已有的 images表上添加一个 compatible_path，可以为空
        2. 前端 ImageContet 如果有该路径则前端在 prefer=origin 的时候应该优先显示该路径图片。如果失败则落回local_path，如果还失败就落回缩略图，要仔细考虑这里的状态转换
        3. 下载导入管线对于浏览器不支持格式（前端会用 [types](packages\image-type\src\index.ts) 探测），后端改成写定支持的图片格式，然后在前端探测之后找到浏览器不支持的格式。对于视频则内置不支持列表wmv、mov和mkv等保守列表，因为前端不检测。视频通常只需要视频轨和音频轨即可够用
        4. 探索代码库如果还有没有确定的问题请提问
    ```
- [ ] 优化事件转发管线（节流+队列），避免事件风暴
- [ ] 图片详情 ImageDetailContent路径上的每一段都能直接点，打开对应文件夹
    - 进一步地，直接抽象一个anchor组件，可以用来打开本地和远程路径，适配各种url
- [ ] 美化应用图标体系（Recraft AI ）
- [x] `4.4.0` 将task cancelled 改成result失败原因枚举中，而非字符串推断
- [ ] 视频封面生成，优化画廊视频显示
- [ ] surf 和 crawler 爬虫窗口图标优化
- [x] `4.3.0` 连续删除的时候性能变差（第一次删除很快，第二次要等一两秒，需要定位原因）

## feat
- [x] `4.2.0` 自动同步文件夹画册

### 图片
- [ ] 图片标签
- [ ] 图片评分
- [ ] 图片改名
- [x] `4.2.0` 图片详情处点击跳转过滤器功能

- [x] `4.2.0` 组合式搜索排序，在header下用一整行io组件来实现
    - [x] 插件provider所有自动加上filter_comb目标
    - [x] 插件provider的resolve最好为了避免歧义，用tag_之类的前缀
- [ ] 图片内容特征（rgb颜色特征）+ 按特征排序

### 画册
- [ ] 画册搜索功能
- [ ] 画册更多排序方法
- [ ] 树状画册浏览

### 应用
- [ ] 操作撤销功能
- [ ] 添加托盘操作
    - [ ] 壁纸过渡方式切换
    - [ ] 壁纸填充方式切换
- [x] `4.2.0` 自动github更新，浏览小漫画查看更新日志
- [ ] ! 局域网数据共享（复用爬虫插件，通过http访问peer，并提供可扩展的发现协议）
    - 可以抽象一个应用数据融合功能，从而能够把融合目标改成本地或者远程
- [ ] 应用数据导出、导入功能
- [ ] ! Provider 支持远程 URL（Provider 可以返回远程图片 URL，支持预览模式和按需下载）
桌面右键菜单下一张图片（windows用nsis脚本，plasma用desktop文件），并且做好错误处理
- [x] `4.2.0` 关闭壁纸功能，这种情况下不会设置壁纸
- [x] `4.4.0` MCP 管理页面、更详细的 MCP 资源说明（尤其是provider）
    - MCP功能开关
- [ ] ! library 分库管理
- [ ] 多显示器支持

### 插件
- [x] `4.4.0` 插件能够请求打开webview窗口，等待用户登录，降低需cookie爬虫的难度
    - 用更简单的方式，从surf获取cookie，新增了 requireCookie 杰阔
- [ ] !! comfyui插件

### 杂项
- [x] `4.2.0` 安卓 photoswipe 管理错误状态（图片丢失占位）和加载中状态 
    > 直接用自定义html解决问题
- [ ] 安卓用桌面小组件代替桌面上系统托盘功能。
- [ ] 文件资源管理器快速预览插件（macos quiklook, windows preview handler，gnome sushi, KDE KIOpreview）
- [ ] 安卓前台服务

## refactor
- [x] `4.2.0` 吉祥物气泡消息
- [x] `4.3.0` 从rhai转到js插件, node 子进程或者 rusty_v8 两种技术方案待选择（直接给AI喂Deno core的源代码）
- [ ] 脚本化爬取流程，自动爬取用脚本化爬取代替

## stash
