---
description: 排查问题：动手实测 + 调用链跟踪优先于纯源码分析
alwaysApply: true
---

# 排查问题：实践出真知

源码只能告诉你**代码可能怎么走**；跑起来才能告诉你**它实际怎么走了**。排查 bug 时，纯静态推理极易在第一步就跑偏，然后把后续所有工作建在错误前提上。

## 铁律

1. **先量状态，再解释现象。** 在解释「为什么它不工作」之前，先测量对象的**实际状态**。窗口不显示 → 先量它的尺寸/样式/父子关系/可见性，而不是先推理合成管线。控件没数据 → 先打印它收到的实际入参，而不是先读数据源代码。
   > 反例（真实事故）：壁纸窗口不显示，从日志里一个 `EX_STYLE=0x200110` 推导出「DWM 合成问题」，一路推到「patch CEF + 重编 Chromium」。真相是窗口只有 158×26（以最小化状态创建），一次窗口枚举就能看见。**从头到尾没人问过「这窗口到底有多大」。**

2. **跟真实调用链，每一跳都验证实际值。** 顺着 app → runtime → 第三方库 → 系统 API 逐跳走，在每个边界打印/断点确认**真实传递的值**，不要假设「这里应该是 X」。跨语言/跨进程边界（Rust↔CEF、前端↔Tauri、主进程↔子进程）是最容易出错的地方，也最值得插桩。
   > 真实事故的正解就是这样找到的：`.visible(false)` → runtime 译成 `ShowState::HIDDEN` → CEF 在非 macOS 上把它翻成 `kMinimized`。三跳，每跳都有源码，但只有把实际 style 位读出来（`WS_MINIMIZE` 置位）才锁定是哪一跳出的问题。

3. **零成本实验优先。** 能用**现有二进制** + 环境变量 / 命令行开关 / 现有日志测出来的，绝不先改代码、更不要先重编依赖。先问：「有没有办法两分钟内证伪这个假设？」
   - 本仓库现成的口子：`KABEGAME_CEF_GPU_MODE`、Chromium 的 `--disable-features=<Name>`、已有的 `[DEBUG-*]` 日志、`cocs/debug/DEBUG_INGEST.md` 的 runtime debug ingest。

4. **假设必须可证伪，且预先写下判据。** 设计能把假设**劈开**的实验，动手前先写「如果结果是 X，就说明假设错了」。没有判据的实验只会用来确认偏见。
   - 排成阶梯：外科手术式（只动一个变量）→ 大锤式（关掉整个子系统）。最重的一级都不亮 → 假设错了，**停下重新诊断**，不要继续加码。

5. **引用证据必须逐字，不许掺解读。** 贴源码就贴原文，自己的推断写在引用**外面**。
   > 反例（真实事故）：在引用的 CEF 源码行尾加了一句源码里根本没有的 `// ← no NOREDIRECTIONBITMAP` 注解，这条伪证据把整份实施计划带偏。

6. **成本越高的结论，证据要求越高。** 结论若指向「patch 第三方库 / 重编 Chromium / 大规模重构」，先停下来问：**支撑它的最强证据是什么？我实测过吗？** 高成本路线几乎总有一个更便宜的证伪实验没做。

## 与其他规则的关系

- 日常改代码的校验走 [verify-by-lint.mdc](mdc:.cursor/rules/verify-by-lint.mdc)（跑 check，不跑 build）。
- **本规则是它的例外**：排查 bug、或需要确认「改动在真实运行时是否生效」时，**应该**真正跑起来观察——lint 证明不了运行时行为。
