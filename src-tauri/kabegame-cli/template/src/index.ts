import { to, query, getAttr, downloadImage, addProgress } from "@kabegame/plugin-sdk";

export async function crawl(common: any, custom: Record<string, unknown>) {
  // TODO: 在这里编写爬取逻辑

  addProgress(100.0);
}
