{
    "namespace": "kabegame",
    "name": "gallery_search_native_metadata_query_provider",
    "properties": {
        "query": { "type": "string", "default": "", "optional": false }
    },
    "query": {
        "join": [{
            "kind": "LEFT",
            "table": "image_metadata",
            "as": "native_metadata_im",
            "on": "images.image_metadata_id = native_metadata_im.id",
            "in_need": true
        }],
        "where": "LOWER(native_metadata_im.search_text) LIKE LOWER('%' || REPLACE(REPLACE(REPLACE(${properties.query}, '\\', '\\\\'), '%', '\\%'), '_', '\\_') || '%') ESCAPE '\\'"
    },
    "resolve": {
        ".*": {
            "delegate": { "provider": "gallery_route" },
            "child_var": "child",
            "provider": "${child.provider}",
            "meta": "${child.meta}"
        }
    }
}
