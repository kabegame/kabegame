{
    "$schema": "../schema.json5",
    "namespace": "kabegame",
    "name": "vd_year_ko_provider",
    "properties": {
        "year": { "type": "string", "default": "", "optional": false }
    },
    "query": {
        "delegate": {
            "provider": "year_provider",
            "properties": {
                "year": "${properties.year}"
            }
        }
    },
    "list": {
        "${root.name}월": {
            "delegate": {
                "provider": "year_provider",
                "properties": {
                    "year": "${properties.year}"
                }
            },
            "child_var": "root",
            "provider": "vd_month_ko_provider",
            "properties": {
                "year_month": "${properties.year}-${root.name}"
            },
            "meta": {
                "month": "${root.name}",
                "label": "${root.name}월"
            }
        },
        "${page.name}": {
            "child_var": "page",
            "delegate": {
                "provider": "page_size_provider",
                "properties": { "page_size": 100 }
            },
            "provider": "query_page_provider",
            "properties": {
                "page_size": 100,
                "page_num": "${page.meta.page_num}"
            },
            "meta": "${page.meta}"
        }
    }
}
