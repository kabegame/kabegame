# v4.4.0 changelog

## Added
- **插件**:
  - 添加标签系统，可以声明自己是否需要翻墙、需要cookie
  - 可以声明应用从畅游处cookie加到请求中(requireCookie)，插件读不了cookie。对于需要cookie的插件，能直接指示应用从畅游处获取cookie发送
  - 添加每任务隔离的插件私有虚拟文件系统：V8 提供完整 `deno_fs` API，WebView 提供无句柄子集；插件卸载时 best-effort 清理其 data、cache、tmp 目录，升级时保留
  - 新增[haowallpaper](https://www.haowallpaper.com)、[小红书](https://www.xiaohongshu.com)、[kemono](https://kemono.cr) 插件。小红书webview插件实现了关键字检索+滚动下载。参考项目 [XHS-Downloader](https://github/JoeanAmier/XHS-Downloader)
  - 新增archive接口，能够解压zip、gzip、bzip、7z、tar，使用方法是通过原生vfs接口来解压，然后遍历图片文件downloadImage导入。
  - 新增 WebView 页面自发原生下载出口：文件落到窗口 VFS `tmp/Downloads`，插件可监听终态事件并统一导入；畅游自动导入普通文件，并探测解压 ZIP、TAR 与 7z 后逐项导入。
  - 新增ffmpeg接口，可以用来混合m4s流
  - 新增changelog收集和展示（本质上是markdown渲染）。
- **CLI**: 新增 plugn run 命令，能够运行一个已安装插件，先import安装kgpg插件，然后run，是当前的v8插件的开发、测试工作流（集成Claude skill）。
- **原生元数据**:
  - 新增图片格式原生元数据查看：JPEG 读 EXIF（拍摄参数、GPS、缩略图、MakerNote，含 GPS 隐私提示），PNG 走自写 chunk walker（IHDR、tEXt/iTXt/zTXt 文本块、色彩、物理尺寸/时间、未知块），能直接看到 ComfyUI 的 `prompt`/`workflow`、A1111 的 `parameters`、NovelAI 的生成参数（进一步可以通过MCP帮忙检索所有带生成参数的图片）
  - 扩展支持 WebP 与 GIF：WebP 走 RIFF chunk walker（VP8X 容器标志与画布、VP8/VP8L/ALPH 位流、ICCP、XMP、ANIM/ANMF 动画聚合），其 EXIF 与 JPEG 同构故共用同一套分组；对 EXIF chunk 带 `Exif\0\0` 前缀的文件（多见于 JPEG 转码而来）额外做一次剥前缀重试，避免解析不出。GIF 走自写 block walker（画布与全局调色板、帧数/总时长/循环次数、Comment、XMP、ICC），只按长度跳过数据子块、不解 LZW
- **整理**： 新增补充原生元数据的选项
- **图片详情**: 预览窗左右侧栏宽度可拖动（240px ~ min(560px, 45vw)，双击复位，分别持久化）
- **隐藏图片清理**: 新增一键清空隐藏画册；桌面源文件分批移入系统回收站并对失败块逐条降级，Android 优先直删 app 自有 MediaStore 媒体，其余在 Android 11+ 合并为一次系统删除授权。
- **MCP**:
  - 新增只读发现工具 `list_pathql_entry`，可逐层枚举 PathQL 懒树、读取节点说明并分页发现子节点；资源模板由 10 条扩展到 15 条。
  - 新增 images 集合分页护栏：未显式分页且超过 500 行的读取返回 `pagination_required`，避免一次拉取过大结果集。
- **PathQL 查询**: 
  - 新增随机排序，可以随机抽图玩啦
  - 新增元数据搜索，可以直接通过搜索prompt来找AI图片了，但在那之前别忘了执行一次整理来填充元数据
  - 更自由强大的过滤！你可以组合高级过滤来实现例如找到元数据里有 prompt 但名称里没有 Comfy 的图片。高级过滤能显示pathql路径，让你知道机箱下面是什么在工作。
  - 搜索新增两个维度：**按本地路径**（匹配图片在本机的保存路径，`\` 与 `/` 通用）和**按链接**（匹配图片直链或来源页链接，占位/本地导入的假 URL 不参与）。画廊、画册详情与高级查询可用；任务/畅游详情保持原有三项。
- **文件夹画册**: 文件夹画册可以转换为普通画册

## Fixed
- **壁纸**: 
  - Windows \ MacOS 窗口模式壁纸不显示的bug
  - 视频壁纸不自动播放bug
- **任务**:
  - 安卓爬虫任务进度条不显示的bug
  - 取消、失败任务进度条不显示的bug
  - js任务没有记录进度条，只是发送了进度条事件的bug
- **应用背景**:
  - 应用侧边栏没有显示背景图的bug
- MacOS点击图标不显示窗口的bug
- **畅游**： 
  - 打开重复host的url的时候，已有窗口会刷新到新url而不只是打开。
  - 修复下载完成后没有 toast 反馈的问题；右键下载、blob/data 导入和页面自发下载的成功/失败终态现在统一提示。
- **画册**: 
  - 无法查看子画册bug
  - 画册设置轮播对象失败bug
  - 面包屑跳转bug
  - 子画册返回bug
- **插件**:
  - 插件文档无法渲染问题
- **文件拖入**: 通过给CEF打补丁补全chromium的drag回调事件。需要重新生成binding和编译chromium
- **PathQL 路径编码**: 理清双层编码契约——pathql 反斜线转义是逻辑层、URL percent 是传输层，各边界恰好解一层。修复了反斜线被解两层（搜含 `\` 的 Windows 路径必失败）、关键词里的 `/` 被当成路径分隔吃掉后续段、字面 `%XX` 被双重解码、以 `~` 开头的关键词撞组合器语法报错等一串问题；引擎的 percent 兜底随之移除。

## Optimized
- **安装**: Windows 安装弹太多黑窗问题
- **界面**:
  - 应用界面主页优化，header部分工具缩减，主要工具放在kamechan里
  - 安卓选择插件变成弹窗，更好看
  - 设置页面去除，改成设置弹窗
  - 帮助页面去除，改成打开官网
  - 畅游页面优化
  - 应用后台任务可以看到进度了
  - 插件页面美化
  - 画册页面美化和简化
- **本地文件夹画册**: 可以取消，可以选择快速导入（比较文件夹是否更新）
- **体验**: 画廊加载全部图片数量不会导致进入加载状态
- **鼠标拖图**: 鼠标拖动预览大图和网格小图都自动拖动原图\视频

## Changed
- **MCP**:
  - Rust MCP 服务升级到 rmcp 3.0，继续使用 StreamableHTTP 与本地 session manager。
  - **行为变更**：URI 无法映射到精确 read capability 时，不再默认放行；仅当同 scheme 仍有任一 read 能力开启时才允许。因此关闭全部 images read 后 `images://` 裸根也会拒绝，未注册 scheme 直接拒绝。
- **畅游**： 下载不再脱离并发限制，但也不阻塞交互，而是进队，之后调度webview开启原生下载。
- **图片详情**: 详情区拆成基本信息、原生元数据、插件简介三个同构面板
- **文档**: Demo 页面url改成 demo.kabegame.com
- **插件**: 插件要声明白名单的文档asset列表，而不会自动递归查找
- **Web**： Web下首页默认改成随机正序
- **界面**: 搜索框被移动到到过滤行中
- **文件拖入**: 松手即导入，去掉了原来的导入确认弹窗；拖入提示层从全屏居中卡片改成贴合目标区域的一圈虚线 + 框内一层淡色蒙层 + 中间静态图标，去掉了呼吸动画。`.kgpg` 现在只在源页面接受（原来在任何位置都能装）。
  - 拖入从「窗口级一刀切」改成**区域级**——拖到哪个页面区域，就由那个区域决定接什么、怎么导入。桌面 CEF 后端补齐了 drag over/leave/drop 三个回调，落点坐标经命中判定路由到热区：
    - **画廊**（网格区）：图片与视频导入画廊；文件夹按目录树创建递归同步画 册。
    - **画册详情**：图片与视频导入并直接加入当前画册；文件夹在当前画册下建成子画册。本地文件夹同步画册、收藏、隐藏三类画册整体拒绝拖入。
    - **画册列表**：只接受文件夹，创建根级递归同步画册。
    - **源页面**：只接受 `.kgpg` 插件包。
    - 任务详情与畅游详情不接受拖入。
- **文件夹画册**: 现在文件夹画册不能和普通画册混合，新增迁移会自动将它们铺到顶层并自动成树

## Removed
- **插件**: 移除kgpg v2兼容，迁到kgpg v3，文件头包含50kb的固定icon数据
- **文档**: 
  - README 改成指向 kabegame.com 网站
  - 去掉帮助页，文档统一收束。
- **工具**:
  - 快捷帮助抽屉去掉
  - 快捷设置抽屉去掉
- **过滤**:
  - 按名称分桶过滤
  - 按是否设置过壁纸过滤
  - 点击图片名称跳转名称过滤

## 对更新者：
1. 如果你原图库较大（上万张），首次启动可能会等几十秒到几分钟等待元数据的搜索字段回填，这个时候可能看不到转圈，因为还没有做初始化前的UI交互，请耐心等待一会~
2. 建议对全画廊运行一次整理，勾选回填原生元数据。
