# v4.4.1 regression

本版回归 checklist。任何改动可预见的回归路径，要在上线前 check 完毕。
按「操作」一步步点，对照「预期」，通过就把第一列勾上。

## 简单过滤叠加高级条件

自动化：`deno task test -c kabegame --skip cargo` 覆盖查询模型、chip 交互和 facet 计数路径。
Linux CEF 已实测示例 OR 叠加、应用、重新加载、独立清除与再次打开；四页与 Android 真机仍按下表回归。

| 是否完成 | 标题 | 环境 | 操作 | 预期 | 备注 |
| --- | --- | --- | --- | --- | --- |
| [x] | 空高级弹窗 | Linux CEF | 无追加条件时打开高级 chip；清空高级条件后再次打开 | 不默认展示空条件行，只显示「添加条件」「添加或组」入口 | Vitest 同时覆盖桌面和紧凑布局的空查询入参 |
| [ ] | 查询条布局与独立编辑 | 桌面四种图库页面 | 设置插件和比例后点击最右侧高级 chip | 无模式开关和查询条路径；简单 chip 始终显示；弹窗只有追加条件，首次为空白 | Vitest 覆盖组件布局与弹窗入参 |
| [ ] | AND 叠加 OR 条件 | 桌面 | 设置 Pixiv + 横图，追加「原生元数据 sakura 或图片」并应用 | 路径为简单条件后接 `~any/search/native-metadata/sakura/~or/media-type/image/~end`；高级弹窗计数包含简单过滤 | Vitest 覆盖示例路径 |
| [ ] | 独立清除与取消 | 桌面 / Android | 点击高级 chip 的清除按钮，再打开；修改草稿后取消；另试清除全部 | 高级清除仅移除追加条件，不打开弹窗；再打开为空白；取消不改查询；清除全部同时清两部分 | Vitest 覆盖主体/清除事件、紧凑布局和取消 |
| [ ] | 简单条件仍可编辑 | 桌面 / Android | 高级条件生效时修改搜索或简单维度，Android 从折叠菜单打开过滤 | 高级条件保留，简单条件正常更新，计数与最终结果一致 | Vitest 覆盖搜索及简单插件清除 |
| [ ] | 查询边界持久化 | 桌面 | 分别追加单个原子、OR、NOT；刷新，前进后退，再编辑高级条件 | 简单与高级条件不会互相迁移；没有简单条件时高级单原子也保持独立 | Vitest 覆盖归一化与 URL 往返 |
| [ ] | 路由上下文 | 桌面四种图库页面 | 配合 hide/no-album、排序、分页使用追加条件 | 根路径、隐藏状态、排序和分页正确；修改过滤回到第 1 页 | Vitest 覆盖画册/任务/畅游路径往返 |
| [ ] | 插件细分与空条件计数 | 桌面 | 在高级弹窗选择插件细分；启用 no-album 后展开无条件的简单 facet | 高级提示仅保留插件，细分可在简单 chip 设置；空计数使用 no-album 叶，不请求不存在的 filter_comb/all | 引擎的组合组不支持插件细分的 fields 贡献；facet 空计数有 Vitest 回归 |

## 搜索「任意」tab

自动化：`deno task test -c kabegame --skip cargo` 覆盖任意搜的序列化展开、解析折叠、与简单/高级条件的边界及 detail 路由往返。

| 是否完成 | 标题 | 环境 | 操作 | 预期 | 备注 |
| --- | --- | --- | --- | --- | --- |
| [ ] | 任意搜命中并集 | 桌面画廊 | 搜索 chip 切到最前面的「任意」tab，输入一个只出现在链接或元数据里的词 | 能搜到；结果数 ≥ 任一单模式的结果数；说明文字列出覆盖的五个 tab | 路径为 `~any/search/display-name/<q>/~or/…/~end` |
| [ ] | 往返保持 tab | 桌面 | 任意搜生效后刷新、前进后退、复制路径重新打开 | 搜索 chip 仍显示「任意」徽章和原关键词，不会变成高级 chip 里的或组 | Vitest 覆盖折叠 |
| [ ] | 受限页范围 | 任务详情 / 畅游详情 | 在任意 tab 搜索 | 只覆盖基础三项（名称/元数据/原生元数据），说明文字同样只列三项；计数与列表一致 | 范围写入时固化进搜索项 |
| [ ] | 叠加其它维度 | 桌面四种图库页面 | 任意搜 + 插件/日期/比例 chip，再展开各维度 facet | facet 计数正常，不报路由错误；清除搜索立即生效 | 验证 `~any…~end/<root>/` 上下文前缀可路由 |
| [ ] | 高级弹窗条件行 | 桌面 / Android | 高级弹窗里的搜索条件切到「任意」并应用；再切回单模式 | 生效并与简单条件 AND；切回后路径不残留其它模式分支 | |
| [ ] | 切换模式 | 桌面 | 有关键词时在「任意」与单模式间来回切 | 每次切换立刻重查并回到第 1 页 | |

## 爬虫运行配置选择

| 是否完成 | 标题 | 环境 | 操作 | 预期 | 备注 |
| --- | --- | --- | --- | --- | --- |
| [ ] | 运行配置展示 | Windows / macOS / Linux | 打开“开始采集”对话框，展开第一行的已有配置下拉框，并选择一个带描述的配置 | 选项与选中值显示“插件名称 - 配置名称”，配置名称使用原描述的较小次要文字样式；长名称不撑宽或溢出下拉框，悬浮选项或选中值时通过浏览器原生提示显示完整描述 | 无描述的配置不设置 `title`、不显示空白提示 |
| [ ] | 紧凑布局展示 | Android | 打开“开始采集”抽屉，展开第一行的已有配置选择器 | 配置项显示“插件名称 - 配置名称”，不再直接显示描述 | 分别检查短名称和超长名称 |

## 打开文件 / 定位文件

本版把「打开文件」「打开所在文件夹」「打开目录」这几个入口的底层实现整个换掉了，
外部表现应当与上版完全一致。涉及入口：图片详情的「打开文件」、图片右键的「打开所在
文件夹」、画册页与画册详情的本地文件夹与画册盘目录、设置页的当前壁纸与默认下载目录。

| 是否完成 | 标题 | 环境 | 操作 | 预期 | 备注 |
| --- | --- | --- | --- | --- | --- |
| [ ] | 打开图片 | Windows | 画廊里选一张图 → 图片详情 → 点「打开文件」 | 系统默认看图程序打开这张图；**过程中不闪黑色命令行窗口** | 换一个视频再试一次 |
| [x] | 打开图片 | macOS / Linux | 同上 | 系统默认程序打开这张图 | |
| [ ] | 打开所在文件夹 | Windows | 图片上右键 → 「打开所在文件夹」 | 资源管理器打开，并且**这张图被选中高亮**，不是只打开了父目录 | |
| [ ] | 打开所在文件夹 | macOS | 同上 | Finder 打开并选中这张图 | |
| [x] | 打开所在文件夹 | Linux | 同上 | 文件管理器打开并选中这张图 | 少数桌面环境不支持选中，此时应退化为「打开父目录」，不能直接报错 |
| [x] | 打开本地文件夹画册 | Windows / macOS / Linux | 画册页找一个本地文件夹画册 → 点打开目录 | 文件管理器**新开一个窗口**，定位到该文件夹 | |
| [x] | 打开画册盘挂载点 | Windows / macOS / Linux | 先挂载画册虚拟盘 → 画册页或画册详情点挂载点 | 文件管理器打开挂载盘根目录 | 需要先装好虚拟盘驱动并挂载 |
| [ ] | 打开画册盘子目录 | Windows / macOS / Linux | 找一个**带子画册**的画册 → 在虚拟盘中打开它的子画册 | 直接定位到对应的多级子目录，而不是停在盘根 | 多级路径是这次容易踩的点 |
| [ ] | 特殊路径 | Windows | 把图片放到路径含**空格、中文**的目录下，重复上面「打开图片」「打开所在文件夹」「打开本地文件夹画册」 | 三项都和普通路径一样正常 | 应用内部会把路径规范化，长路径前缀是这次的风险点 |
| [x] | 设置页入口 | Windows / macOS / Linux | 设置 → 点「当前壁纸」打开 / 点「默认下载目录」打开 | 前者用默认程序打开壁纸图片，后者打开该目录 | |
| [ ] | Android 打开 | Android | 设置 → 默认下载目录点打开；图片详情点「打开文件」 | 弹出系统应用选择器并成功打开，**不再固定提示「暂不支持」** | Android 上「打开所在文件夹」依旧不支持，点了报错属预期 |
| [ ] | 异常路径 | 全平台 | 找一张源文件已被删除或移动的图片 → 点「打开文件」和「打开所在文件夹」 | 弹中文错误提示 toast，应用不卡死不崩溃 | |
| [ ] | 入口全量点检 | 任一桌面平台 | 把上面列到的每个入口都实际点一遍 | 没有任何一个入口提示「命令不存在」或「没有权限」 | 这次改了命令名，漏改的地方只会在运行时暴露，编译不会报错，所以必须逐个点 |
| [ ] | 画册盘驱动安装 | Windows | 设置里触发安装画册盘驱动 | 正常弹出 UAC 授权并安装成功 | 这条路径本次未改动，属回归兜底确认 |

## 图片预览进度条

| 是否完成 | 标题 | 环境 | 操作 | 预期 | 备注 |
| --- | --- | --- | --- | --- | --- |
| [ ] | 缩放进度布局 | 任一桌面平台 | 打开一张图片预览，将鼠标移到预览底部唤出控制条，再拖动缩放进度条 | 缩放百分比显示在进度条右下方并随缩放实时更新；进度条本身仍与两侧按钮垂直居中 | 分别检查窗口预览和应用内全屏 |
| [ ] | 视频进度布局 | 任一桌面平台 | 打开一段视频预览，将鼠标移到预览底部唤出控制条，再播放并拖动播放进度条 | 当前时间/总时长显示在进度条右下方并随播放或拖动实时更新；进度条本身仍与两侧按钮垂直居中 | 分别检查窗口预览和应用内全屏 |

## 预览控制条显隐

| 是否完成 | 标题 | 环境 | 操作 | 预期 | 备注 |
| --- | --- | --- | --- | --- | --- |
| [ ] | 悬停唤出与延迟隐藏 | 任一桌面平台 | 打开图片或视频预览，将鼠标移入底部热区，再移出热区和控制条 | 移入后控制条立即显示；移出后保持约 1 秒再隐藏，鼠标在倒计时内重新移入控制条时不会闪退 | Vitest 已覆盖热区、控制条与延迟计时逻辑 |
| [ ] | 暂停后延迟隐藏 | 任一桌面平台 | 打开视频预览，暂停播放并将鼠标移出控制条 | 暂停时控制条立即显示，约 1 秒后自动隐藏，不会因视频处于暂停状态而一直常驻 | Vitest 已覆盖暂停事件与延迟隐藏逻辑 |
| [ ] | 进度条焦点下左右切换 | 任一桌面平台 | 打开含多项内容的预览，点击图片缩放条或视频进度条后按左右箭头 | 左右箭头切换上一项或下一项，不调整当前滑块数值 | Vitest 已覆盖滑块对左右箭头默认行为的阻止 |
| [ ] | 控制条响应式宽度 | 任一桌面平台 | 打开图片或视频预览，拖动左右详情栏改变中间预览容器宽度，并分别经过 1000px、300px、200px 三个宽度 | 控制条最宽 600px；容器为 1000px 时占 60%，继续收窄时控制条同步缩小且占比持续增大，到 300px 时占满 100%；小于 200px 时控制条与唤出热区完全隐藏，重新变宽后恢复 | 同时检查普通预览与应用内全屏 |

## CEF 窗口尺寸约束

| 是否完成 | 标题 | 环境 | 操作 | 预期 | 备注 |
| --- | --- | --- | --- | --- | --- |
| [ ] | 主窗口最小尺寸 | Windows / macOS / Linux | 启动桌面应用，拖动主窗口边缘持续缩小 | 内容区不能缩到 `800×600` 以下；到达下限后继续拖动不再改变尺寸 | 分别在 100% 与高 DPI 缩放下检查 |
| [ ] | 创建期最大尺寸 | Windows / macOS / Linux | 创建一个设置了 `max_inner_size` 的测试窗口，持续拖动窗口边缘放大 | 内容区不能超过配置的最大宽高 | 同时检查只限制一个轴的配置 |
| [ ] | 运行时更新约束 | Windows / macOS / Linux | 对已显示窗口依次调用 `set_min_size`、`set_max_size` 和 `set_size_constraints`，每次调用后拖动窗口边缘 | 新约束立即生效；传入 `None` 清除对应约束后可再次越过旧边界 | 检查逻辑尺寸与物理尺寸入参 |

## Linux ozone 后端与拖出文件

本版把 Linux 的 ozone 后端从「固定 X11」改成「按会话探测」：`WAYLAND_DISPLAY` 有值就跑
原生 Wayland，否则仍是 X11。**X11 会话的行为与上版完全一致**（探测结果就是 x11），
需要重点验的是 Wayland 会话——那是本版新走通的路径，整个窗口、输入、GPU 栈都换了后端。
`KABEGAME_OZONE_PLATFORM=x11|wayland` 可强制覆盖，用来对照。

另外新增了 `/download/<绝对路径>` 端点专供拖出下载，修掉「拖出去的文件都叫 `file`、
没有扩展名、后一次覆盖前一次」——路径改为直接接在端点后面，文件名因此落在 URL 末段。
`/file` **未改动**，页面里的图片与视频仍走它，显示路径不受影响。该端点只挂在桌面路由，
web 与 Android 不开放；路径白名单与 `/file` 完全一致（必须真实存在且能在 images 表查到）。

**macOS 的拖出本版不修**，在这里挂一条记录，去 macOS 上继续这项工作时从它开始：那边拖出去
拿到的是一串 URL 而不是文件。根因与 Linux 同源——渲染进程发起的拖拽交不出真实本地路径
（`RenderWidgetHostImpl::FilterDropData` 会把 `did_originate_from_renderer` 的 `filenames`
整个清空，这是安全边界，不是 bug），所以只能退化成 URL。根治要让浏览器进程自己发起拖拽并
给出真实路径，但 macOS 是 `NSPasteboard` / `NSDraggingSession` 另一套实现，与 Linux 的
`OSExchangeData` + aura drag client 不能共用，需单独做。三平台里只有 Windows 现状是好的
（`DownloadURL` → 真虚拟文件）。

| 是否完成 | 标题 | 环境 | 操作 | 预期 | 备注 |
| --- | --- | --- | --- | --- | --- |
| [x] | 后端探测 | Linux Wayland 会话 | 启动应用，`pgrep -af kabegame-cef-helper \| grep -o '\-\-ozone-platform=[a-z0-9]*'` | 全部子进程都是 `--ozone-platform=wayland` | 主进程用 `xwininfo -root -tree \| grep -i kabegame` 应查不到窗口 |
| [ ] | 后端探测 | Linux X11 会话 | 同上 | 全部子进程都是 `--ozone-platform=x11`，行为与上版一致 | 这是回归兜底：X11 会话不能因本次改动有任何变化 |
| [x] | 强制覆盖 | Linux Wayland 会话 | `KABEGAME_OZONE_PLATFORM=x11` 启动 | 退回 X11，窗口重新出现在 `xwininfo` 树里 | 逃生阀，Wayland 下出问题时要能退回去 |
| [ ] | 拖出到文件管理器 | Linux Wayland 会话 | 图库里拖一张图到 Dolphin / Nautilus 窗口 | 光标显示可放下（不是禁止符号），松手后文件落地 | 上版在 Wayland 桌面下恒为禁止符号 |
| [ ] | 拖出文件名 | Linux Wayland 会话 | 连续拖出两张**不同**的图到同一个目录 | 两个文件都带正确扩展名、用的是库里的原文件名，**不互相覆盖** | 本次新增 `/download` 端点修的就是这个 |
| [ ] | 拖出中日文文件名 | Linux Wayland 会话 | 拖出一张文件名含中文或日文的图（例如带【】和省略号的） | 落地文件名不是乱码、不被截断 | 名字**只**由 URL 末段给出；`Content-Disposition` 恒为不带 filename 的 `attachment`，不参与命名。编码往返已有前端单测 `fileUrl.test.ts` 覆盖，这条验的是真实目标端解出来的名字 |
| [ ] | 现有视图不受影响 | 全平台 | 浏览图库网格、打开图片详情、播放视频、切换原图/缩略图偏好 | 显示一切如常 | `/file` 本次未改动，这条是兜底确认 |
| [ ] | 端点白名单 | 任一桌面平台 | 直接访问 `http://127.0.0.1:<port>/download/etc/passwd` | 返回 404，不返回文件内容 | 白名单语义不得放宽：必须能在 images 表里按路径查到 |
| [ ] | 端点不开放 | web | 启动 web 版，访问 `/download/...` | 404 / 路由不存在 | 该端点只挂在桌面 `file_routes()` |
| [ ] | Windows 拖出 | Windows | 从图库拖一张图到资源管理器 | 落地文件名与扩展名正确 | Windows 走 `DownloadURL`，URL 换成 `/download` 后要回归确认 |
| [ ] | macOS 拖出 | macOS | 从图库拖一张图到 Finder 窗口或桌面 | 落地一个真实文件 | **当前不满足：落地的是一串 URL。本版不修**，macOS 侧工作从这条开始；先确认换成 `/download` 后现象有无变化 |
| [ ] | 拖出内容正确 | Linux Wayland 会话 | 拖出一张图后，比对落地文件与库内原图的 sha256 | 完全一致；拿到的是**原图**而不是缩略图 | 网格里拖的是缩略层，别拖出缩略图 |
| [ ] | 拖入文件 | Linux Wayland 会话 | 从文件管理器拖若干图片进各个拖入热区 | 拖入照旧可用，浮层与命中区域正常 | 换了后端，`TauriCefDragHandler` 的坐标契约要重验 |
| [ ] | 窗口身份 | Linux Wayland 会话 | 看任务栏与 Alt-Tab | 只有一个条目、图标正确，不出现双条目 | Wayland 下靠 app_id，不再是 `WM_CLASS` |
| [ ] | 渲染与视频 | Linux Wayland 会话 | 浏览图库、播放视频壁纸、开畅游窗口 | 画面正常，无黑屏、无花屏；视频能出声自动播放 | 日志里 `Frame latency is negative` 属已知无害告警 |
| [ ] | 托盘 | Linux Wayland 会话 | 检查托盘图标与菜单 | 图标出现，菜单可点 | 托盘走 GTK，`GDK_BACKEND` 本版跟随 ozone 一起切到 wayland |
| [ ] | 多显示器与缩放 | Linux Wayland 会话 | 在不同缩放比的显示器间移动窗口 | 不模糊、不错位；拖入热区命中仍然准 | Wayland 的缩放语义与 X11 不同，这里最容易出偏移 |
| [ ] | 壁纸设置 | Linux Wayland 会话 | 设置一张静态壁纸和一张视频壁纸 | 设置成功并生效 | 壁纸走桌面环境接口，与 ozone 后端无关，属兜底 |

## Linux 拖出真实本地文件

本节验证的是浏览器进程授权并注入真实本地路径的新链路，与上一节的 ozone 会话探测和
`/download` URL 文件名修复是两批独立改动，不能用上一节的结果代替。

**前置条件：必须先应用 `third-patches/cef/0003-drag-source-filenames.patch`，用它重新构建
CEF/Chromium，再用该 CEF 构建并运行应用。** 如果仍使用旧 CEF，新的 delegate 回调根本不会被
调用，拖拽会退化成 Blink 为 `<img>` 自动填充的缩略图载荷——前端在 Linux 上已不再写
`DownloadURL` / `text/uri-list` / `text/plain`，没有 HTTP URL 兜底了。这表示前置条件未满足，
不是本功能失效。

除明确写为 Windows 的用例外，下面每条 Linux 用例都必须在 **原生 Wayland 与 X11 会话各跑
一遍**，不能只用 `KABEGAME_OZONE_PLATFORM` 在同一会话下代替完整的双会话验证。

| 是否完成 | 标题 | 环境 | 操作 | 预期 | 备注 |
| --- | --- | --- | --- | --- | --- |
| [ ] | 绘图软件画布 | Linux Wayland / X11（各一次） | 从图库分别拖一张图到 GIMP 与 Krita 的画布或打开区域 | 两个程序都直接接收并打开/导入本地原图，不要求程序下载 localhost URL | 选择一张原图与缩略图肉眼可区分的图片 |
| [ ] | Firefox 上传框 | Linux Wayland / X11（各一次） | 从图库拖图到 Firefox 测试页的 `<input type="file">` | 上传框选中该本地文件，文件名与原文件一致；不是 URL 文本 | 用只在本机运行的最小测试页即可，不需要实际上传 |
| [ ] | Chrome 上传框 | Linux Wayland / X11（各一次） | 从图库拖图到 Chrome 测试页的 `<input type="file">` | 上传框选中该本地文件，文件名与原文件一致；不是 URL 文本 | 与 Firefox 分开验证，两者的拖放接收实现不同 |
| [ ] | 文件管理器复制 | Linux Wayland / X11（各一次） | 从图库拖图到 Nautilus 或 Dolphin 目录；再拖一次触发同名冲突 | 文件按本地文件的**复制**语义落地，同名时走文件管理器自己的冲突处理；不创建下载任务、不显示网络下载行为 | Wayland 与 X11 至少各选一个当前会话原生的文件管理器 |
| [ ] | 文本编辑器 | Linux Wayland / X11（各一次） | 把图片拖到支持文件/URI 拖放的文本编辑器（如 Kate） | 编辑器收到本地 `file://` URI 或本地路径并按自身规则打开/插入；不得收到 `http://127.0.0.1` | 不要求文本编辑器能渲染图片，只检查它识别到的来源 |
| [ ] | 关闭 HTTP server 后拖出 | Linux Wayland / X11（各一次） | 在调试环境停止或阻断应用本地 HTTP server，先确认原 `/download/...` URL 已无法访问，再从图库重拖到文件管理器和一个浏览器上传框 | 两个目标仍成功收到文件 | 这是证明新链路不再依赖下载的关键用例；应用进程与 CEF 必须保持运行 |
| [ ] | 第三方页面伪造 custom mime | Linux Wayland / X11（各一次） | 在畅游的第三方内容页中，用测试元素的 `dragstart` 写入 `application/x-kabegame-image-id`，值取 DB 中真实存在的图片 id，再拖到外部目标 | 拖拽中**不带本地文件**：没有 filename / `file://` 项；若页面原本带 URL，只能保留普通 URL 载荷 | 必须用真实 id，才能证明拦截来自 webview label 白名单而不是 DB 查询失败 |
| [ ] | 不存在的 image id | Linux Wayland / X11（各一次） | 在 `main` 的调试会话中把 custom mime 值替换为 DB 中不存在的 id，再拖到外部目标 | 拖拽中**不带本地文件**：没有 filename / `file://` 项；Linux 上也不再有 HTTP URL 回落，目标端只会拿到 Blink 默认的缩略图载荷 | 同时确认应用不崩溃、拖拽线程不卡死 |
| [ ] | 原图内容校验 | Linux Wayland / X11（各一次） | 选择一张已生成不同内容缩略图的图片，拖到文件管理器，分别对库内原图、落地文件和缩略图计算 sha256 | 落地文件 sha256 与库内原图完全一致，并与缩略图不同 | 验证 `file_contents` 没有让目标静默优先消费缩略图字节 |
| [ ] | Linux 载荷不含 localhost URL | Linux Wayland / X11（各一次） | 用最小 GTK 接收器（`Gtk.DropTarget`）打印全部 offered mime 与 `text/uri-list` 逐行内容 | 不出现任何 `http://127.0.0.1` 项；`text/uri-list` 第一行即 `file://` 指向库内原图 | 前端已不再写 `DownloadURL` / `text/uri-list` / `text/plain`，这条直接证明载荷来源只剩注入的 filename |
| [ ] | Windows DownloadURL 回归 | Windows | 从图库拖图到资源管理器及一个支持文件拖放的目标 | 行为与改动前一致，继续通过 `DownloadURL` 交付虚拟文件，文件名、扩展名与内容正确 | Linux resolver 不会安装，Windows 不应进入真实路径注入链路；`/download` 端点与 `downloadToUrl` 为此保留，务必确认未被一并删除 |
| [ ] | 拖入回归 | Linux Wayland / X11（各一次） | 从文件管理器拖若干图片进入画廊、画册等现有拖入热区，依次检查 Enter / Over / Drop / Leave | 浮层、落点命中、导入与离开清理均正常；不会导航到被拖入文件 | 验证 `TauriCefDragHandler` 原有落点侧行为不受新增起手侧回调影响 |

## anime-pictures 插件 Cloudflare 403

`cf_clearance` 与签发它的 UA 绑定。插件原先写死的是 Windows Chrome/124 UA，和畅游（Linux CEF 149）的 UA
不一致，所以即使 cookie 是从畅游新鲜复制来的，列表页仍会 403。现已把 UA 改为畅游的 UA。

| 是否完成 | 标题 | 环境 | 操作 | 预期 | 备注 |
| --- | --- | --- | --- | --- | --- |
| [ ] | 列表页不再 403 | Linux | 在畅游中通过 anime-pictures 的 Cloudflare 验证，把 `cf_clearance` 填入插件后重新打包，运行第 0 页任务 | 列表页返回 200，并能解析出帖子链接 | cookie 与 IP、UA 绑定，必须在同一台机器、同一出口 IP 下获取 |
| [ ] | 使用畅游 Cookie | Linux | 在畅游中打开 anime-pictures 并通过验证，然后运行任务 | 日志出现「已从畅游注入 anime-pictures.net 的 Cookie」 | 畅游没有该站记录时会 warn 并回退到内置 Cookie |
| [ ] | `cefUserAgent` 各平台取值 | Windows / macOS / Linux | 运行 anime-pictures 任务，查看日志中的「User-Agent 来源」 | 显示「畅游 CEF」，平台段分别为 `Windows NT 10.0; Win64; x64` / `Macintosh; Intel Mac OS X 10_15_7` / `X11; Linux x86_64`，版本段为 `Chrome/149.0.0.0`，且与该平台畅游里 `navigator.userAgent` 逐字一致 | 在畅游 devtools 执行 `navigator.userAgent` 对照 |
| [ ] | Android 上 `cefUserAgent` | Android | 运行调用 `Kabegame.cefUserAgent()` 的插件 | 返回 `null`，插件回退到内置 UA，不报错 | Android 畅游是系统 WebView，不走 CEF |
| [ ] | V8 snapshot 重建 | 任意桌面 | 升级后首次运行任意 V8 插件 | 生成 `runtime@6.bin` 并正常运行；`Kabegame.cefUserAgent` 可调用 | prelude.js 有改动，fingerprint 已从 5 升到 6 |
| [ ] | 原图下载 | Linux | 同上任务继续执行到详情页下载 | `api.anime-pictures.net/pictures/download_image/...` 307 到 `oimages`，下载器合并重定向响应里的 `kira=1` 后拿到 `image/jpeg` | 缺 `kira` cookie 时 `oimages` 会 302 回首页，得到的是 HTML |

## 手动刷新重试失败图片

图片加载失败后会在右上角点亮蓝 / 黄 / 红感叹号（缩略图 / 兼容图 / 原图不可达）。原先手动刷新后图片 id 不变、
组件被复用，感叹号和「图片丢失」占位会一直保留。现在手动刷新会让当前页所有图片清空失败标记并重试加载；
事件驱动的自动刷新不受影响。

| 是否完成 | 标题 | 环境 | 操作 | 预期 | 备注 |
| --- | --- | --- | --- | --- | --- |
| [ ] | 恢复后刷新消除感叹号 | Windows / macOS / Linux | 把某张图的本地文件临时改名，在画廊中让它显示红色感叹号或丢失占位；把文件改回原名，点刷新按钮（或按刷新快捷键） | 该图正常显示，感叹号消失 | 缩略图也要一起挪走，才能稳定触发失败 |
| [ ] | 文件仍缺失 | Windows / macOS / Linux | 同上，但不恢复文件就刷新 | 重新走一遍回退链后，再次显示同样的感叹号 / 丢失占位；不报错、不卡骨架屏 | |
| [ ] | 画册 / 任务详情 | Windows / macOS / Linux | 在画册详情和任务详情页各重复第一条 | 行为与画廊一致 | 两页都走同一个 `ImageGrid.refresh` |
| [ ] | 刷新不闪、状态保留 | Windows / macOS / Linux | 选中几张图、展开画册树或过滤树后手动刷新 | 正常加载的图片不闪、不重新播放入场动画；顶部查询栏和侧栏树的状态不重置 | 本次没有整体重新挂载网格，只重置失败状态 |
| [ ] | 自动刷新不重试 | Windows / macOS / Linux | 保持一张图处于失败状态，触发一次图片变更事件（如在别处删除另一张图） | 该图的失败标记保留，不反复重试 | 只有手动 `refresh` 会递增 `reloadEpoch` |
| [ ] | Android 刷新 | Android | 在画册详情或任务详情页下拉刷新一张已失败的图 | 与桌面一致 | Android 的图片层判定是「原图层失败即丢失」 |

## 高级查询删除分支残留「或」

在或组里添加分支再删掉该分支的条件后，分支本身没有被删除，留下一个空分支，UI 上多出一条悬空的「或」分隔线。
现在删掉分支里最后一个条件会同时删除该分支；最后一个分支也删空时，整个或组一并移除。

| 是否完成 | 标题 | 环境 | 操作 | 预期 | 备注 |
| --- | --- | --- | --- | --- | --- |
| [ ] | 删分支不残留「或」 | 任意 | 高级查询中添加或组，点「添加分支」加出第三个分支，再点该分支条件右上角的删除 | 分支消失，末尾不再多一条「或」，只剩两个分支和一条「或」 | |
| [ ] | 删中间分支 | 任意 | 三个分支时删掉中间那个的条件 | 剩余两个分支顺序不变，编号、条件内容正确 | |
| [ ] | 删到只剩零分支 | 任意 | 依次删掉或组所有分支的条件 | 最后一个删掉后整个或组消失；已取非的或组同样消失，不残留空的「取非」框 | |
| [ ] | 查询结果一致 | 任意 | 删除分支后看画廊结果与 URL | 结果与只含剩余分支的查询一致，URL 中不出现空分支片段 | |

## ImageGrid 去掉受控模式、surface 更名 adapter

纯重构：`ImageGrid` 的 `surface` prop 改为必传的 `adapter`，删除 `images` 受控模式；`imageGrid/surfaces/` → `imageGrid/adapters/`，`GridSurfaceAdapter` → `GridAdapter`。行为不应变化。

| 是否完成 | 标题 | 环境 | 操作 | 预期 | 备注 |
| --- | --- | --- | --- | --- | --- |
| [ ] | 四种图库页加载与翻页 | 桌面 / Android | 分别打开画廊、画册页 / 画册详情、任务详情、畅游详情，翻页、改每页条数、手动刷新 | 列表与总数正常，页码与 URL `?path=` 一致 | `usePagedGallery` 返回值直接解构暴露 |
| [ ] | keep-alive 返回 | 桌面 | 画廊 → 任务详情 → 返回画廊；再在其它页改变画廊路径后返回 | 返回后列表按当前路由路径刷新，无空白页 | `onActivated` 重载分支已去掉 adapter/paged 判空 |
| [ ] | 事件刷新与右键菜单 | 桌面 | 在各页删除、隐藏、加入画册、收藏图片；预览中跨页左右切换 | 当前页自动刷新，确认框文案按页面区分，预览跨页正常 | |

## 本地文件夹监听/同步双管道

| 是否完成 | 标题 | 环境 | 操作 | 预期 | 备注 |
| --- | --- | --- | --- | --- | --- |
| [ ] | 大目录删图后事件仍畅通 | Linux，43k 文件同步目录 | 从画廊删除一张本地文件夹图片，随后隐藏和删除其它图片 | 画廊立即刷新；日志中不出现整目录事件重扫 | 文件事件只走 diff |
| [ ] | 单文件落盘 | Windows / macOS / Linux | `cp` 一张媒体文件到 shallow/recursive 同步目录 | 约 1.5 秒后图片出现，不显示卡片、不弹 toast | |
| [ ] | 大批量落盘与取消 | Windows / macOS / Linux | 一次复制 5000 张图片，卡片出现后取消 | 卡片显示真实百分比；取消后不再继续导入，未扫描项不被删除 | |
| [ ] | 新目录首扫 | Windows / macOS / Linux | recursive 画册下新建目录并立即复制图片 | 自动建立子画册并完成首次导入 | 先 reconcile 监听、再首扫 |
| [ ] | 删除子目录 | Windows / macOS / Linux | 对已同步子目录执行 `rm -r` / 删除 | 子画册及子树消失；其中图片行仍在图库 | 逐文件删除事件不得删行 |
| [ ] | 重命名文件 | Windows / macOS / Linux | 重命名同步目录内媒体文件 | 旧路径图片行删除，新路径图片导入 | |
| [ ] | 根目录消失 | Windows / macOS / Linux | 移走根同步目录或拔掉所在卷 | 根画册及子树删除，图片行保留，并显示警告 toast | 拔盘同样触发该语义 |
| [ ] | 无变化启动 | Windows / macOS / Linux | 正常退出后重新启动且不改目录 | 不显示同步卡片、不弹 toast | |
| [ ] | 手动同步反馈 | Windows / macOS / Linux | 右键同步未变化画册；再执行两种递归同步 | 单层任务提示“未变化”；递归任务逐目录显示，可单独或全部取消 | |
| [ ] | 关闭快速同步时手动同步无变化 | Windows / macOS / Linux | 设置里关闭快速同步，右键同步一个无变化的画册 | 弹出「已同步：新增 0、删除 0、更新 0」，不能无反馈 | 手动任务不论有无变化都提示 |
| [ ] | 刷新只同步当前层 | Windows / macOS / Linux | Albums 页选中本地文件夹画册并点刷新 | 刷新立即返回，只投递当前画册 `descend=none` | |
| [ ] | 事件队列溢出 | 调试环境 | 人工制造 inotify/FSEvents/ReadDirectoryChangesW 队列溢出 | 只记录明确日志，不偷偷触发全量扫描 | |

## 畅游一键下载与页面快照

自动化：`test-kabegame kabegame-core --lib metadata_search_text_override` 覆盖快照行的 search_text 只含标题与 URL；发现 / 快照脚本已用 happy-dom 冒烟（srcset 最大项、懒加载、og/JSON-LD/JW 配置、m3u8 与 blob 过滤、脚本与 `on*` 清除）。以下需桌面真机回归。

| 是否完成 | 标题 | 环境 | 操作 | 预期 | 备注 |
| --- | --- | --- | --- | --- | --- |
| [ ] | 检测与下载 | Windows / macOS / Linux 畅游 | 打开图片较多的页面，点导航栏最右侧下载按钮 | 按钮转圈；先 toast“正在检测媒体资源”，再 toast“检测到 N 个媒体资源（图片 a / 视频 b）”；图库陆续出现图片；结束 toast“已加入 x/N”并停止转圈 | 下载受并发上限约束，入队逐个进行 |
| [ ] | 无媒体页面 | 桌面 | 在纯文字页面点按钮 | toast“未检测到可下载的媒体资源”，转圈立即结束 | |
| [ ] | 转圈中取消 | 桌面 | 媒体很多的页面，转圈时再点一次按钮 | 点击后立刻停止转圈；toast“已取消”；此后不再有新项入队，已入队的继续完成 | Rust 侧 `collectRunId` 校验兜底 |
| [ ] | 取消后重新开始 | 桌面 | 取消后立刻再点一次 | 新一轮正常检测和下载，旧一轮不再入队 | |
| [ ] | 整页导航打断 | 桌面 | 转圈时点击页面链接跳到另一页 | 转圈停止；新页可再次一键下载 | `on_page_load(Started)` 清 run |
| [ ] | 页面未就绪 | 桌面 | 页面刚开始加载时立即点按钮 | 地址栏短暂显示“页面尚未就绪”，不转圈 | |
| [ ] | 不暴露全局 | 桌面 | 在内容页 devtools 执行 `Object.keys(window).filter(k => /kabegame|collect|discover|snapshot/i.test(k))` | 不出现一键下载相关的新全局（仅既有的 `__kabegame_toast` 等） | |
| [ ] | 快照开启（默认） | 桌面 | 全新数据目录下确认设置「下载」分区的“冻结畅游页面”默认打开；一键下载后打开任一图片详情 | 面板显示页面标题、刷新按钮与“打开原网页”；下方 iframe 渲染页面快照且样式基本还原；同批图片的快照相同 | 同批共享 1 行 metadata；已存过 false 的旧数据目录保持原值 |
| [ ] | 刷新快照 | 桌面 | 快照内图片加载失败或滚动后，点快照上方的刷新按钮 | iframe 重新挂载：回到顶部并重新加载远程图片 | |
| [ ] | 快照关闭 | 桌面 | 关闭“冻结畅游页面”，一键下载后打开图片详情 | 详情面板显示空状态，不出现页面快照 | |
| [ ] | 快照安全 | 桌面 | 对含 `<script>`、表单、`onclick` 的页面冻结后回看 | 快照内脚本不执行、链接和表单无法导航或提交 | `sandbox=""` + 内层 CSP |
| [ ] | 打开原网页 | 桌面 | 点击“打开原网页” | 按“链接打开方式”设置打开下载时的页面 URL | |
| [ ] | 快照不污染搜索 | 桌面 | 用页面正文里的某个词搜索（任意 / 元数据） | 不会因快照 HTML 命中无关图片；用页面标题可以搜到 | search_text 只含标题与 URL |
| [ ] | 右键下载不受影响 | 桌面 | 开启冻结后在图片上右键下载 | 行为与之前一致，不生成快照 | |

## 网页收集（内置 `webpage`）

自动化：`test-kabegame kabegame-core --lib webpage` 覆盖 URL / 后端 / Header 校验、注入开关默认值、内建插件固定字段、`discoverMedia` 在 deno_dom 静态文档上的 fixture（srcset 最大项、懒加载、`<base>`、fragment 去重、og / JSON-LD / 直链 / 脚本直链、m3u8 过滤）与 V8 模块整链冒烟；`--lib page_snapshot` / `--lib metadata_search_text` 覆盖快照上限与只索引标题 URL；`deno task test -c kabegame --skip cargo` 覆盖表单提交形状（WebView 强制空 Header、V8 开关默认开、Android 只有 V8）与「打开 WebView」按钮判定。以下需真机回归。

| 是否完成 | 标题 | 环境 | 操作 | 预期 | 备注 |
| --- | --- | --- | --- | --- | --- |
| [ ] | 三来源入口 | 桌面工具栏下拉 / 空状态对话框 / Android 紧凑选择器 | 分别打开「开始收集」 | 均按「本地 / 网络 / 网页」顺序出现；点「网页」先关选择层再开网页表单，不进入插件选择 | Web 发布版三处都没有「网页」 |
| [ ] | 表单字段来自插件定义 | 桌面 | 打开网页表单 | URL、后端、两个注入开关的标签来自内置插件（切换语言随之变化）；默认后端 V8，两开关默认打开 | |
| [ ] | URL 校验 | 桌面 / Android | 分别提交空、`example.com`、`ftp://a.com`、`https://u:p@a.com` | 不创建任务，URL 字段下显示对应原因；改正后可提交 | 后端 `start_task` 另有二次校验 |
| [ ] | V8 静态收集 | 桌面 / Android | 对图片较多的静态页提交 V8 任务 | 任务来源显示「网页收集」；图库陆续出现图片；日志有「发现 N 个媒体」「已提交 x/N」 | |
| [ ] | 入口即媒体 | 桌面 | URL 直接填一张图片地址 | 只下载这一张；详情快照为只含该图片的最小页面 | |
| [ ] | 自定义 HTTP 头 | 桌面 | V8 模式填 `Referer` 后提交，查看任务参数 | 任务 Header 带上该头，页面与媒体请求都生效；填 `Host` 或含换行的值时无法创建任务 | Header 值不出现在日志 |
| [ ] | 自动注入畅游 Cookie / UA | 桌面 | 先在畅游登录某站，再对其需要登录的页面提交 V8 任务 | 日志有「已使用畅游浏览器的 User-Agent」「已从畅游注入 … 的 Cookie」，能拿到登录态内容 | |
| [ ] | 关闭注入开关 | 桌面 | 同上页面关闭两个开关再提交 | 日志无注入记录，以匿名身份请求 | |
| [ ] | 显式 Header 优先 | 桌面 | 两开关打开，同时在 Header 里手填 `User-Agent` 与 `Cookie` | 保留手填值，不被畅游值覆盖 | |
| [ ] | 无畅游记录 | 桌面 | 对从未在畅游打开过的站点提交 V8 任务 | 只 warn「畅游无该站 Cookie」，任务照常完成 | |
| [ ] | WebView 动态收集 | 桌面 | 对瀑布流 / JS 动态加载的页面提交 WebView 任务 | 运行中任务抽屉与任务详情出现「打开 WebView」，打开可见页面在自动滚动；约 20 秒内结束滚动并收集滚动后出现的媒体 | V8 任务、pending 与终态任务不显示该按钮 |
| [ ] | WebView 表单 | 桌面 | 先在 V8 下填 Header，再切到 WebView | Header 编辑器与两个注入开关立即隐藏；提交后任务 Header 为空；切回 V8 草稿仍在 | |
| [ ] | WebView 登录态 | 桌面 | 在需要登录的站点，打开任务 WebView 手动登录后重新提交 | 媒体用浏览器会话下载成功 | 下载走 CEF 原生通道 |
| [ ] | 冻结开启（默认） | 桌面 | 分别跑 V8 与 WebView 任务，打开任一图片详情 | 显示页面标题、刷新与「打开原网页」；V8 快照无样式、WebView 快照样式基本还原；同页图片共享同一快照 | 「打开原网页」打开用户提交的初始 URL |
| [ ] | 冻结关闭 | 桌面 / Android | 设置「下载」里关闭「冻结网页」再跑任务 | 图片详情无页面快照 | Android 设置页现在也显示该开关 |
| [ ] | 快照超限 | 桌面 | 对超大页面（HTML > 32MB）运行 | 日志 warn「页面快照未保存」，媒体照常下载 | |
| [ ] | post_url | 桌面 | 对会重定向的 URL 运行后看图片信息 | 来源链接是用户提交的初始 URL，而非重定向后地址 | |
| [ ] | 取消 | 桌面 | V8 取页中、WebView 滚动中、等待下载中分别取消 | 任务进入已取消，此后不再新增入库 | |
| [ ] | Android 仅 V8 | Android | 打开网页表单 | 后端只有 V8，并提示 WebView 仅桌面可用；无目标文件夹、无注入开关 | |
| [ ] | 保留 ID | 桌面 | 尝试安装 id 为 `webpage` 的 kgpg | 安装被拒绝；来源管理、插件选择器中不出现网页收集 | |
| [ ] | 畅游一键下载不受影响 | 桌面 | 畅游中点一键下载 | 行为与之前一致（发现脚本已迁至 core，改为运行时拼接） | |
